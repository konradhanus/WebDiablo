{
  "name": "webdiablo",
  "version": "1.11.0",
  "private": true,
  "description": "WebDiablo — single-file dark-fantasy dungeon crawler. Runtime has ZERO dependencies; devDependencies are test-only.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-check.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "test:e2e": "node tests/e2e.mjs",
    "test": "npm run build && npm run test:unit && npm run test:e2e",
    "release": "node scripts/release.mjs"
  },
  "devDependencies": {
    "playwright": "^1.40.0"
  }
}
